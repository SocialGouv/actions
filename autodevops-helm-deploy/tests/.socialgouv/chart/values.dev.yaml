components:
  jobs: true

hasura:
  needs: [db]

pgweb:
  needs: [db]

jobs:
  runs:
    - name: db
      # use: SocialGouv/actions/uses/create-db@v1
      use: ../uses/create-db
      with:
        pgAdminSecretRefName: pg-scaleway
