apiVersion: v1
kind: Service
metadata:
  labels:
    component: maildev
    application: {{ .Values.global.repositoryName }}
  name: maildev
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - name: http
      port: {{ .Values.servicePort }}
      targetPort: 8081
  selector:
    component: maildev
  type: ClusterIP
  